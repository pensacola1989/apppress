Admin.HeaderView = Em.View.extend({
    templateName: 'header',
    didInsertElement: function() {
//    	if (Util.isNotEmpty(Vari.UserEmail)) {
//    		$("#userEmail").text(Vari.UserEmail);
//    	}
    }
});