Client.Router.map(function () {
    Client.Router.map(function () {
        this.resource('index', { path: '/'});

        this.resource('main', { path: '/main' }, function() {
        });
    });
});


